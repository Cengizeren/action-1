name: Dummy Workflow

on:
  workflow_dispatch:
  pull_request:
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Hello World
        id: validate
        run: |
          echo "Hello World"           fi
